# iDo 2.0 - Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            iDo 2.0 ARCHITECTURE                              │
└─────────────────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────────────┐
│                         FRONTEND (Browser)                                 │
├───────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐       │
│  │   LEFT SIDEBAR   │  │   CENTER AREA    │  │  RIGHT SIDEBAR   │       │
│  ├──────────────────┤  ├──────────────────┤  ├──────────────────┤       │
│  │                  │  │                  │  │                  │       │
│  │ Mini Calendar    │  │  Week Header     │  │  Profile Info    │       │
│  │  ┌──┬──┬──┬──┐  │  │  ◄ Dec 2025 ►    │  │  👤 User Name    │       │
│  │  │ 1│ 2│ 3│ 4│  │  │                  │  │  email@mail.com  │       │
│  │  └──┴──┴──┴──┘  │  │                  │  │                  │       │
│  │                  │  │  Calendar Grid   │  │  ┌──────────┐    │       │
│  │ Today Section    │  │  ┌──┬──┬──┬──┐  │  │  │  Tasks   │ +  │       │
│  │  📅 Events       │  │  │Mo│Tu│We│Th│  │  │  └──────────┘    │       │
│  │  ✓ Tasks         │  │  ├──┼──┼──┼──┤  │  │                  │       │
│  │                  │  │  │  │  │  │  │  │  │  📝 Planned      │       │
│  │ Filters          │  │  │  │  │  │  │  │  │   - Task 1       │       │
│  │  • All           │  │  │  │  │  │  │  │  │   - Task 2       │       │
│  │  • Personal      │  │  └──┴──┴──┴──┘  │  │                  │       │
│  │  • Work          │  │                  │  │  📋 Unplanned    │       │
│  │                  │  │                  │  │   - Task 3       │       │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘       │
│                                                                            │
│  ┌──────────────────────────────────────────────────────────────────────┐ │
│  │                         BOTTOM BAR                                    │ │
│  │  Status: ✓ Synced                                    🔄 Sync          │ │
│  └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
└───────────────────────────────────────────────────────────────────────────┘

                                    ▼ ▼ ▼

┌───────────────────────────────────────────────────────────────────────────┐
│                        JAVASCRIPT MODULES                                  │
├───────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐               │
│  │   app.js     │    │ calendar.js  │    │  drive.js    │               │
│  ├──────────────┤    ├──────────────┤    ├──────────────┤               │
│  │              │    │              │    │              │               │
│  │ • Init app   │◄──►│ • Fetch GCal │    │ • OAuth      │               │
│  │ • Add tasks  │    │ • Render week│    │ • Load tasks │               │
│  │ • Toggle     │    │ • Mini cal   │    │ • Save tasks │               │
│  │ • Delete     │    │ • Today sect │    │ • User info  │               │
│  │ • Render UI  │    │ • Drag/drop  │    │              │               │
│  │              │    │              │    │              │               │
│  └──────┬───────┘    └──────┬───────┘    └──────┬───────┘               │
│         │                   │                   │                        │
│         └───────────────────┼───────────────────┘                        │
│                             │                                            │
│                    ┌────────▼────────┐                                   │
│                    │ taskSchema.js   │                                   │
│                    ├─────────────────┤                                   │
│                    │ • Task model    │                                   │
│                    │ • Validation    │                                   │
│                    │ • Helpers       │                                   │
│                    └─────────────────┘                                   │
│                                                                            │
└───────────────────────────────────────────────────────────────────────────┘

                                    ▼ ▼ ▼

┌───────────────────────────────────────────────────────────────────────────┐
│                         GOOGLE APIS                                        │
├───────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  ┌────────────────────┐  ┌────────────────────┐  ┌─────────────────────┐ │
│  │  Google Drive API  │  │ Google Calendar    │  │  Google OAuth 2.0   │ │
│  ├────────────────────┤  ├────────────────────┤  ├─────────────────────┤ │
│  │                    │  │                    │  │                     │ │
│  │ • Read/Write       │  │ • Read Events      │  │ • Authentication    │ │
│  │   ido-data.json    │  │   (Read-only)      │  │ • PKCE Flow         │ │
│  │                    │  │                    │  │ • Refresh Tokens    │ │
│  │ Stores:            │  │ Returns:           │  │                     │ │
│  │  {                 │  │  - Event title     │  │ Scopes:             │ │
│  │    tasks: [        │  │  - Start/end time  │  │  • drive.file       │ │
│  │      {             │  │  - All-day flag    │  │  • calendar.readonly│ │
│  │        id,         │  │  - Location        │  │  • userinfo.email   │ │
│  │        text,       │  │                    │  │  • userinfo.profile │ │
│  │        done,       │  │                    │  │                     │ │
│  │        dueDate,    │  │                    │  │                     │ │
│  │        priority    │  │                    │  │                     │ │
│  │      }             │  │                    │  │                     │ │
│  │    ]               │  │                    │  │                     │ │
│  │  }                 │  │                    │  │                     │ │
│  │                    │  │                    │  │                     │ │
│  └────────────────────┘  └────────────────────┘  └─────────────────────┘ │
│                                                                            │
└───────────────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
                              DATA FLOW DIAGRAM
═══════════════════════════════════════════════════════════════════════════════

1. USER LOGIN
   ┌──────┐
   │ User │──► Click "Sign in with Google"
   └──────┘
      │
      ▼
   ┌─────────┐
   │ OAuth   │──► Redirect to Google
   │ Flow    │
   └─────────┘
      │
      ▼
   ┌─────────┐
   │ Google  │──► User grants permissions:
   │         │    • Drive access
   │         │    • Calendar read
   │         │    • Profile info
   └─────────┘
      │
      ▼
   ┌─────────┐
   │ Tokens  │──► Store in localStorage
   │ Saved   │    • Access token
   └─────────┘    • Refresh token


2. INITIAL LOAD
   ┌──────────┐
   │ app.js   │──► initializeApp()
   └──────────┘
      │
      ├──► Fetch user info ──────────────┐
      │                                   ▼
      │                          ┌──────────────┐
      │                          │ Display      │
      │                          │ Profile Info │
      │                          └──────────────┘
      │
      ├──► Load tasks from Drive ────────┐
      │                                   ▼
      │                          ┌──────────────┐
      │                          │ Render Tasks │
      │                          │ Sidebar      │
      │                          └──────────────┘
      │
      └──► Initialize calendar ──────────┐
                                         ▼
                                ┌──────────────┐
                                │ Fetch GCal   │
                                │ Events       │
                                └──────────────┘
                                         │
                                         ▼
                                ┌──────────────┐
                                │ Render       │
                                │ Calendar Grid│
                                └──────────────┘


3. DRAG-AND-DROP TASK SCHEDULING
   ┌──────┐
   │ User │──► Drag task from sidebar
   └──────┘
      │
      ▼
   ┌────────────┐
   │ Drag Start │──► Store taskId
   └────────────┘    Add .dragging class
      │
      ▼
   ┌────────────┐
   │ Drag Over  │──► Show visual feedback
   │ Calendar   │    Add .drag-over class
   └────────────┘
      │
      ▼
   ┌────────────┐
   │ Drop on    │──► Get date from column
   │ Day Column │
   └────────────┘
      │
      ▼
   ┌────────────┐
   │ Update     │──► task.dueDate = newDate
   │ Task Date  │
   └────────────┘
      │
      ▼
   ┌────────────┐
   │ Save to    │──► PUT to Google Drive
   │ Drive      │
   └────────────┘
      │
      ▼
   ┌────────────┐
   │ Re-render  │──► • Calendar grid
   │ All Views  │    • Tasks sidebar
   └────────────┘    • Today section


4. ADD NEW TASK
   ┌──────┐
   │ User │──► Type task text
   └──────┘    Optional: Set due date
      │
      ▼
   ┌────────────┐
   │ Create     │──► TaskSchema.createTask()
   │ Task Obj   │    • Generate UUID
   └────────────┘    • Set timestamps
      │               • Assign properties
      ▼
   ┌────────────┐
   │ Add to     │──► tasks.push(newTask)
   │ Array      │
   └────────────┘
      │
      ▼
   ┌────────────┐
   │ Categorize │──► dueDate ? Planned : Unplanned
   └────────────┘
      │
      ▼
   ┌────────────┐
   │ Render     │──► Show in appropriate section
   │ Sidebar    │
   └────────────┘
      │
      ▼
   ┌────────────┐
   │ Save to    │──► await saveTasks()
   │ Drive      │
   └────────────┘
      │
      ▼
   ┌────────────┐
   │ Update     │──► If task has date, show on calendar
   │ Calendar   │
   └────────────┘


═══════════════════════════════════════════════════════════════════════════════
                           COMPONENT INTERACTIONS
═══════════════════════════════════════════════════════════════════════════════

   ┌────────────────────────────────────────────────────────────────┐
   │                      Global State                              │
   ├────────────────────────────────────────────────────────────────┤
   │                                                                 │
   │  window.tasks = [...]         ◄───► app.js & calendar.js       │
   │  window.calendarEvents = [...] ◄───► calendar.js only          │
   │  window.currentWeekStart       ◄───► calendar.js only          │
   │                                                                 │
   └────────────────────────────────────────────────────────────────┘

   ┌────────────────────────────────────────────────────────────────┐
   │                  Global Functions (exports)                    │
   ├────────────────────────────────────────────────────────────────┤
   │                                                                 │
   │  FROM app.js:                                                  │
   │    • window.toggleTask(id)                                     │
   │    • window.renderTasksSidebar()                               │
   │    • window.saveTasksToCloud()                                 │
   │                                                                 │
   │  FROM calendar.js:                                             │
   │    • window.initCalendar()                                     │
   │    • window.renderCalendar()                                   │
   │    • window.renderMiniCalendar()                               │
   │    • window.renderTodaySection()                               │
   │    • window.previousWeek()                                     │
   │    • window.nextWeek()                                         │
   │    • window.goToToday()                                        │
   │    • window.handleTaskDragStart(e)                             │
   │    • window.handleTaskDragEnd(e)                               │
   │                                                                 │
   └────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
                              FILE DEPENDENCIES
═══════════════════════════════════════════════════════════════════════════════

   index.html
      │
      ├──► style.css (all styles)
      │
      ├──► config.js (OAuth credentials)
      │
      ├──► taskSchema.js (task data model)
      │     │
      │     └──► Used by: app.js, drive.js
      │
      ├──► drive.js (Google APIs)
      │     │
      │     └──► Exports: window.DriveAPI.*
      │
      ├──► calendar.js (calendar integration) NEW!
      │     │
      │     ├──► Depends on: config.js
      │     ├──► Depends on: window.tasks (from app.js)
      │     ├──► Depends on: window.TaskSchema
      │     └──► Exports: window.initCalendar(), etc.
      │
      └──► app.js (main application)
            │
            ├──► Depends on: config.js, taskSchema.js
            ├──► Depends on: window.DriveAPI (from drive.js)
            ├──► Depends on: window.initCalendar() (from calendar.js)
            └──► Exports: window.toggleTask(), window.saveTasksToCloud()


═══════════════════════════════════════════════════════════════════════════════
                           RESPONSIVE BREAKPOINTS
═══════════════════════════════════════════════════════════════════════════════

   Desktop (1200px+)
   ┌─────────────────────────────────────────────────────────────┐
   │  [  Left  ] [     Center Calendar      ] [   Right   ]      │
   │  [ 280px  ] [         1fr              ] [   320px   ]      │
   └─────────────────────────────────────────────────────────────┘
   • Full three-pane layout
   • 6 day columns (Mon-Sat)
   • All features visible


   Tablet (768px - 1200px)
   ┌─────────────────────────────────────────────────────────────┐
   │  [              Center Calendar Only                 ]      │
   │  [                    100%                           ]      │
   └─────────────────────────────────────────────────────────────┘
   • Left/right sidebars hidden
   • Calendar takes full width
   • Focus on scheduling


   Mobile (<768px)
   ┌───────────────────┐
   │   Calendar Grid   │
   │   (3 columns)     │
   ├───────────────────┤
   │   Tasks List      │
   │   (stacked)       │
   └───────────────────┘
   • Single column layout
   • Calendar shows 3 days
   • Simplified UI


═══════════════════════════════════════════════════════════════════════════════
                              SECURITY MODEL
═══════════════════════════════════════════════════════════════════════════════

   ┌─────────────────────────────────────────────────────────────┐
   │                   OAuth 2.0 with PKCE                       │
   ├─────────────────────────────────────────────────────────────┤
   │                                                              │
   │  1. Generate code_verifier (random 128 chars)               │
   │  2. Generate code_challenge (SHA-256 hash)                  │
   │  3. Redirect to Google with challenge                       │
   │  4. User authorizes                                         │
   │  5. Receive authorization code                              │
   │  6. Exchange code + verifier for tokens                     │
   │  7. Store access_token + refresh_token                      │
   │                                                              │
   └─────────────────────────────────────────────────────────────┘

   Token Storage:
   • localStorage (client-side)
   • XSS risk: Mitigated by CSP headers (recommended)
   • No server-side storage needed


═══════════════════════════════════════════════════════════════════════════════
                           PERFORMANCE OPTIMIZATIONS
═══════════════════════════════════════════════════════════════════════════════

   1. Calendar Events Caching
      • Fetched once per week view
      • Not re-fetched on task changes
      • Only refreshed when week changes

   2. Debounced Saves
      • isSaving flag prevents concurrent saves
      • Multiple rapid changes queued
      • Single API call after changes settle

   3. Minimal DOM Updates
      • Only affected components re-render
      • Use of DocumentFragment for batch updates
      • Event delegation where possible

   4. Lazy Loading
      • Calendar events only fetched when needed
      • Profile info cached in localStorage
      • Tasks loaded once on init


═══════════════════════════════════════════════════════════════════════════════
                               FUTURE ROADMAP
═══════════════════════════════════════════════════════════════════════════════

   Phase 3.0 (Potential Features):
   ┌────────────────────────────────────────────────────────────┐
   │  • Write access to Calendar (create events from tasks)     │
   │  • Recurring tasks                                         │
   │  • Task categories/tags                                    │
   │  • Collaboration (share tasks)                             │
   │  • Mobile app (React Native)                               │
   │  • Offline mode (Service Worker)                           │
   │  • Dark mode toggle                                        │
   │  • Keyboard shortcuts                                      │
   │  • Subtasks                                                │
   │  • Monthly calendar view                                   │
   └────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════

This architecture provides:
✅ Modular, maintainable code
✅ Clear separation of concerns
✅ Scalable for future features
✅ Backward compatible
✅ Secure authentication
✅ Responsive design
✅ Optimal performance

═══════════════════════════════════════════════════════════════════════════════
